let numPar = Number (prompt("Insira um número par"));
const num = 2;
console.log('O resto da divisão é', numPar%num);

// O resto da divisão sempre será 0
// Ao inserir um número ímpar o resto sempre será 1
